{
  "name": "@mini-proteinpaint/config-system",
  "version": "1.0.0",
  "description": "Type-safe configuration system with Zod validation, migrations, and URL persistence",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
